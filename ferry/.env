# Copy this file to .env to switch from mock data to your real Spring Boot backend.

# false = call the real backend at VITE_API_BASE_URL
# true (or unset)  = use local mock JSON in src/mock/ (default, no backend needed)
VITE_USE_MOCK=false
VITE_API_BASE_URL=/api
