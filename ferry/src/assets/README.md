Drop real trip and traveler photography here and reference it from backend responses or page data.
Mock data currently points to Wikimedia Commons demo photography (see credits
in each JSON file's `sources`/`source` fields) so the app renders realistically
out of the box - swap these paths for your own assets at launch.
